'use strict';

/**
 * Promise that resolves with input
 * @param {*} input input
 * @returns {Promise} resolves with input
 */
const dummyChain = async input => input;


module.exports = {
    dummyChain,
};
